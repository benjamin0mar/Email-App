<%= form_for([@evento, @evento.comentarios.build]) do |f| %>

  <div class="field">
    <%= f.label :contenido %><br>
    <%= f.text_area :contenido, class: "body-comentario" %>
  </div>

  <div class="actions">
    <%= f.submit "Guardar", class:"btn"%>
  </div>

<% end %>
